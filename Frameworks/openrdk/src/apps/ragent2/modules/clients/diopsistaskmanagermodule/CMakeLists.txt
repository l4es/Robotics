IF("${CMAKE_SYSTEM}" MATCHES "Linux")

	RDK_ADD_RAGENT_MODULE(ALL_FILES_RECURSE)

	IF(NOT $ENV{HOME} STREQUAL "/home/d940eb")
		ADD_DEFINITIONS(-D__PC__)
	ENDIF(NOT $ENV{HOME} STREQUAL "/home/d940eb")

ENDIF("${CMAKE_SYSTEM}" MATCHES "Linux")
