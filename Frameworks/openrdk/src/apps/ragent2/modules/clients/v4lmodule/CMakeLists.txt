IF(libv4l_FOUND AND V4L2_FOUND)
	INCLUDE_DIRECTORIES(${libv4l_INCLUDE_DIR})
	INCLUDE_DIRECTORIES(${V4L2_INCLUDE_DIR})
	LINK_DIRECTORIES(${libv4l_LINK_DIRECTORIES})
	SET(LINK_LIBS ${libv4l_LIBRARIES})
	RAGENT_MODULE(AUTO)
ENDIF(libv4l_FOUND AND V4L2_FOUND)
