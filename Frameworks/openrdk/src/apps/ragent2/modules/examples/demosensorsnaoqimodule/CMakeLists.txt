# warning: as rev381 this module need to be updated
# temporarily disabling it
IF(FALSE AND COMPILE_NAO_SECTIONS AND NOT LINUX64)
	IF(ALDEBARAN_FOUND)
		RDK_ADD_RAGENT_MODULE(ALL_FILES)
		INCLUDE_DIRECTORIES(${ALDEBARAN_INCLUDE_DIR})
		LINK_DIRECTORIES(${ALDEBARAN_LINK_DIRECTORIES})
		TARGET_LINK_LIBRARIES(${RDK_THIS_MODULE_NAME} ${ALDEBARAN_LIBRARIES};rdknao)
	ENDIF(ALDEBARAN_FOUND)
ENDIF(FALSE AND COMPILE_NAO_SECTIONS AND NOT LINUX64)

