IF(V4L2_FOUND)
	IF(libv4l2_FOUND)
		RDK_ADD_RAGENT_MODULE(ALL_FILES)
		INCLUDE_DIRECTORIES(${V4L2_INCLUDE_DIR})
		INCLUDE_DIRECTORIES(${libv4l2_INCLUDE_DIR} ${JPEG_INCLUDE_DIR})
		LINK_DIRECTORIES(${libv4l2_LINK_DIRECTORIES} ${JPEG_LINK_DIRECTORIES})
		TARGET_LINK_LIBRARIES(${RDK_THIS_MODULE_NAME} ${libv4l2_LIBRARIES};${JPEG_LIBRARIES})
	ENDIF(libv4l2_FOUND)
ENDIF(V4L2_FOUND)
