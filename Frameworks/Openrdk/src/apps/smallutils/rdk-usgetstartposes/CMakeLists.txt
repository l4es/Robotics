ADD_EXECUTABLE(rdk-usgetstartposes usarsim_getstartposes.cpp)
	TARGET_LINK_LIBRARIES(rdk-usgetstartposes ${RDKCORE_LIBRARIES})
	SET_TARGET_PROPERTIES(rdk-usgetstartposes PROPERTIES COMPILE_FLAGS "${RDKCORE_CPPFLAGS} ${RDKCORE_CXXFLAGS}")
	SET_TARGET_PROPERTIES(rdk-usgetstartposes PROPERTIES LINK_FLAGS "${RDKCORE_LDFLAGS}")

IF(NOT CYGWIN)
INSTALL(PROGRAMS rdk-usgetstartposes DESTINATION bin)
ENDIF(NOT CYGWIN)


